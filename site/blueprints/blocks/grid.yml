name: Grid
icon: grid
preview: fields
wysiwyg: true

fields:
  modules:
    extends: fields/layout

    fieldsets:
      gridItem:
        label: Grid Item
        type: group
        fieldsets:
          gridItem: blocks/grid-item
